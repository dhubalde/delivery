# PostgreSQL for dev/staging/prod; leave unset locally to fall back to SQLite for tests.
DATABASE_URL=postgres://delivery:delivery@localhost:5432/delivery
DJANGO_SECRET_KEY=change-me-in-real-environments
DJANGO_DEBUG=1
